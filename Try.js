// code untuk mencoba function
const findMax = require('./max'); 

// contoh 
const bilangan = [3, 5, 7, 2, 8];
const maks = findMax(bilangan);

console.log(`Nilai Terbesarnya adalah: ${maks}`);  
